# StockMate AI – 프로젝트 유기체 보고서

> 작성일: 2026-03-24
> 대상 버전: master 브랜치 (커밋 기준 최신)

---

## 1. 전체 아키텍처 – 데이터 흐름도

```
┌─────────────────────────────────────────────────────────────────┐
│                      Kiwoom WebSocket API                       │
│  wss://api.kiwoom.com (실전) / wss://mockapi.kiwoom.com (모의)  │
└─────────────────────┬───────────────────────────────────────────┘
                      │ GRP 5~8 (0B 체결·0H 예상체결·0D 호가·1h VI)
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│              websocket-listener  (Python asyncio)               │
│  ws_client.py · redis_writer.py · health_server.py             │
└──────┬──────────────────────────────────────────────┬──────────┘
       │ HSET ws:tick:{code}                          │ LPUSH vi_watch_queue
       │ HSET ws:hoga:{code}                          │
       │ LPUSH ws:strength:{code}                     │
       │ HSET ws:py_heartbeat                         │
       ▼                                              ▼
┌──────────────────────┐              ┌───────────────────────────┐
│        Redis         │              │  api-orchestrator          │
│  (중앙 메시지 브로커)  │◄─────────────│  (Java Spring Boot)        │
└──────────────────────┘              │  S1~S13 전략 스캐너         │
       ▲         │                   │  StrategyService           │
       │         │ LPUSH             │  SignalService             │
       │         │ telegram_queue    │  ViWatchService            │
       │         │                   │  TradingScheduler          │
       │         ▼                   └───────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│                      ai-engine  (Python asyncio)                │
│                                                                 │
│  queue_worker      : telegram_queue 폴링 → 규칙 스코어링         │
│                      → 임계값 미달 → ai_scored_queue (CANCEL)    │
│                      → 임계값 초과 → human_confirm_queue         │
│                                                                 │
│  confirm_worker    : confirmed_queue 폴링 → Claude API 분석      │
│                      → ai_scored_queue 발행                      │
│                                                                 │
│  news_scheduler    : 뉴스 분석 → news:trading_control           │
│  monitor_worker    : 데이터 품질 모니터링                         │
│  overnight_worker  : 오버나잇 포지션 평가                         │
│  strategy_runner   : 독립 전략 스캐너 (선택적)                   │
└──────────────────────────┬──────────────────────────────────────┘
                           │ LPUSH ai_scored_queue
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│              telegram-bot  (Node.js + Telegraf)                 │
│                                                                 │
│  signals.js    : ai_scored_queue 폴링 → Telegram 발송            │
│  startConfirmPoller : human_confirm_queue 폴링 → 인라인 버튼 발송 │
│  commands.js   : /ping /status /signals /perf /candidates 등    │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
                     Telegram 사용자
                   (인라인 ✅/❌ 버튼 응답)
                           │
                           │ confirm_yes → confirmed_queue
                           │ confirm_no  → confirm_pending 삭제
                           ▼
                     ai-engine confirm_worker
```

---

## 2. 모듈별 역할

### 2-1. `websocket-listener/` (Python asyncio)

| 파일 | 역할 |
|------|------|
| `main.py` | 진입점, Redis 연결, ws_client 태스크 기동 |
| `ws_client.py` | Kiwoom WebSocket 연결·LOGIN·구독·재연결, GRP 5~8 할당 |
| `redis_writer.py` | 수신 데이터를 Redis 키에 기록 (tick·hoga·strength·vi·heartbeat) |
| `token_loader.py` | Redis 에서 Kiwoom Bearer 토큰 로드 |
| `health_server.py` | HTTP `/health` 엔드포인트 (기본 포트 8081) |

- GRP 1~4 는 Java api-orchestrator 가 사용하며 GRP 5~8 은 이 모듈 전용 (충돌 없음)
- 장 운영 시간 (평일 07:30~15:35 KST) 외 자동 대기, BYPASS_MARKET_HOURS=true 로 우회 가능

### 2-2. `api-orchestrator/` (Java Spring Boot)

| 컴포넌트 | 역할 |
|----------|------|
| `TradingScheduler` | 크론 기반 전략 스캔 스케줄링 (S1·S3·S5·S6·S7 등) |
| `StrategyService` | 전략 로직 구현 (S1~S13) |
| `SignalService` | 신호 중복 체크, 쿨다운, 일별 상한, Redis 발행 |
| `CandidateService` | ka10029 API 후보 종목 캐시, 전략 태그 기록 |
| `ViWatchService` | vi_watch_queue 소비 → S2 VI 눌림목 전략 실행 |
| `KiwoomWebSocketClient` | GRP 1~4 WebSocket 구독 관리 |
| `TradingController` | REST API (토큰·신호·전략·캘린더·헬스 등) |
| `RedisMarketDataService` | Redis 읽기/쓰기 래퍼 |
| `NewsControlService` | 뉴스 기반 매매 제어 상태 관리 |

### 2-3. `ai-engine/` (Python asyncio)

| 파일 | 역할 |
|------|------|
| `engine.py` | 진입점, 워커 태스크 기동, 헬스서버 (포트 8082) |
| `queue_worker.py` | `telegram_queue` 폴링, 규칙 스코어링, Human Confirm Gate 라우팅 |
| `confirm_worker.py` | `confirmed_queue` 폴링, Claude API 분석, `ai_scored_queue` 발행 |
| `analyzer.py` | Claude API 분석 호출 래퍼 |
| `scorer.py` | 규칙 기반 1차 스코어링, 전략별 임계값, 일별 호출 상한 |
| `redis_reader.py` | Redis 읽기/쓰기 함수 모음 (tick·hoga·vi·confirm 큐) |
| `news_scheduler.py` | 주기적 뉴스 분석, `news:trading_control` 갱신 |
| `monitor_worker.py` | 데이터 품질 이상 탐지 |
| `overnight_worker.py` | 오버나잇 포지션 점수 갱신 |
| `strategy_runner.py` | 독립 전략 스캐너 (ENABLE_STRATEGY_SCANNER=true 시 활성) |

### 2-4. `telegram-bot/` (Node.js + Telegraf)

| 파일 | 역할 |
|------|------|
| `src/index.js` | 봇 초기화, 명령어 등록, 인라인 콜백 핸들러 |
| `src/handlers/signals.js` | `ai_scored_queue` 폴링·발송, `human_confirm_queue` 폴링·컨펌 버튼 |
| `src/handlers/commands.js` | `/ping` `/status` `/signals` `/perf` `/candidates` `/score` 등 명령어 |
| `src/services/redis.js` | ioredis 싱글턴, `popScoredQueue`, `getTickData` |
| `src/services/kiwoom.js` | Kiwoom REST API 호출 (매매 제어 등) |
| `src/utils/formatter.js` | 텔레그램 메시지 포맷팅 |

---

## 3. Redis 키 전체 목록

### 3-1. 실시간 시세 데이터 (websocket-listener → api-orchestrator/ai-engine)

| 키 패턴 | 타입 | TTL | 생산자 | 소비자 | 설명 |
|---------|------|-----|--------|--------|------|
| `ws:tick:{stk_cd}` | Hash | 없음(갱신) | WS Listener | AI Engine | 체결 데이터 (10:현재가, 12:체결강도, ...) |
| `ws:hoga:{stk_cd}` | Hash | 없음(갱신) | WS Listener | AI Engine | 호가잔량 (total_buy_bid_req, total_sel_bid_req) |
| `ws:strength:{stk_cd}` | List | 없음 | WS Listener | AI Engine | 체결강도 이력 (LPUSH, 최근 5개 사용) |
| `vi:{stk_cd}` | Hash | 없음(갱신) | WS Listener | api-orchestrator | VI 발동 정보 |
| `ws:py_heartbeat` | Hash | 30s | WS Listener | AI Engine | Python WS 생존 확인 |

### 3-2. 신호 큐 (메시지 브로커)

| 키 | 타입 | TTL | 생산자 | 소비자 | 설명 |
|----|------|-----|--------|--------|------|
| `telegram_queue` | List | 없음 | api-orchestrator SignalService | ai-engine queue_worker | 거래 신호 (LPUSH → RPOP) |
| `vi_watch_queue` | List | 없음 | WS Listener | api-orchestrator ViWatchService | VI 이벤트 (LPUSH → RPOP) |
| `human_confirm_queue` | List | 없음 | ai-engine queue_worker | telegram-bot signals.js | 인간 컨펌 대기 신호 |
| `confirmed_queue` | List | 없음 | telegram-bot index.js | ai-engine confirm_worker | 인간 승인된 신호 (Claude 분석 대기) |
| `ai_scored_queue` | List | 43200s(12h) | ai-engine (queue_worker/confirm_worker) | telegram-bot signals.js | Claude 분석 완료 신호 |
| `error_queue` | List | 86400s(24h) | ai-engine queue_worker | (수동 확인) | 처리 오류 dead-letter |

### 3-3. Human Confirm Gate 임시 저장

| 키 패턴 | 타입 | TTL | 생산자 | 소비자 | 설명 |
|---------|------|-----|--------|--------|------|
| `confirm_pending:{sig_id}` | String | 1800s(30분) | ai-engine + telegram-bot | telegram-bot callback | 컨펌 대기 신호 페이로드 (버튼 콜백 조회용) |

### 3-4. 제어·상태 키

| 키 | 타입 | TTL | 설명 |
|----|------|-----|------|
| `news:trading_control` | String | 없음 | CONTINUE/CAUTIOUS/PAUSE |
| `news:analysis` | String | 없음 | 최근 뉴스 분석 JSON |
| `news:prev_control` | String | 없음 | 이전 제어 상태 (동기화용) |
| `claude:daily_calls:{yyyymmdd}` | String | 86400s | 일별 Claude 호출 횟수 카운터 |
| `claude:daily_tokens:{yyyymmdd}` | String | 86400s | 일별 Claude 토큰 사용량 |
| `signal:dup:{stk_cd}:{strategy}` | String | TTL 설정 | 신호 중복 방지 |
| `signal:stock_cooldown:{stk_cd}` | String | 설정값(분) | 종목 크로스-전략 쿨다운 |
| `signal:daily_count:{yyyymmdd}` | String | 86400s | 일별 신호 발행 수 |
| `calendar:pre_event` | String | 없음 | 경제 이벤트 임박 여부 |
| `monitor:ws_reconnect_count` | String | 없음 | WS 재연결 횟수 |

### 3-5. 후보 종목 캐시

| 키 | 타입 | TTL | 설명 |
|----|------|-----|------|
| `candidates:{market}` | List | 180s(3분) | 후보 종목 코드 목록 (001:코스피, 101:코스닥) |
| `candidates:watchlist` | Set | 600s(10분) | WS 동적 구독 대상 종목 집합 |
| `candidates:tag:{stk_cd}` | Set | 86400s(24h) | 해당 종목에 발행된 전략 태그 목록 |

### 3-6. 오버나잇·기타

| 키 패턴 | 타입 | TTL | 설명 |
|---------|------|-----|------|
| `user_filter:{chatId}` | String(JSON) | 없음 | 사용자별 전략 필터 목록 |
| `watchlist:{chatId}` | Set | 없음 | 사용자별 관심 종목 |

---

## 4. 데이터 흐름 상세

### 4-1. 정규 매매 신호 흐름 (기존 CANCEL 경로)

```
TradingScheduler (크론)
  → StrategyService.scanXxx()
  → SignalService.processSignal(dto)
      ├─ 중복 체크 (signal:dup:)
      ├─ 종목 쿨다운 (signal:stock_cooldown:)
      ├─ 일별 상한 (signal:daily_count:)
      └─ LPUSH telegram_queue

ai-engine queue_worker
  → RPOP telegram_queue
  → 뉴스 PAUSE 여부 확인 (news:trading_control)
  → _build_market_ctx() : ws:tick·ws:hoga·ws:strength·vi 읽기
  → rule_score() : 전략별 규칙 스코어 계산
  → should_skip_ai() : 임계값 미달?
      ├─ YES → result = CANCEL → LPUSH ai_scored_queue
      └─ NO  → LPUSH human_confirm_queue (Human Confirm Gate)

telegram-bot signals.js
  → RPOP ai_scored_queue
  → action=CANCEL → 무시
```

### 4-2. Human Confirm Gate 흐름 (신규)

```
ai-engine queue_worker
  → rule_score >= threshold
  → LPUSH human_confirm_queue {item + rule_score + market_ctx}
  → SETEX confirm_pending:{sig_id} 1800s payload

telegram-bot signals.js startConfirmPoller
  → RPOP human_confirm_queue
  → SET confirm_pending:{sig_id} EX 1800  (중복 보장)
  → sendMessage(chatId, text, inline_keyboard)
      ┌─ ✅ 분석 진행 → callback_data: confirm_yes:{sigId}
      └─ ❌ 취소      → callback_data: confirm_no:{sigId}

사용자가 버튼 클릭
  ├─ confirm_yes:{sigId}
  │    → GET confirm_pending:{sigId}
  │    → LPUSH confirmed_queue payload
  │    → DEL confirm_pending:{sigId}
  │    → editMessageText "분석 진행 중"
  └─ confirm_no:{sigId}
       → DEL confirm_pending:{sigId}
       → editMessageText "신호 취소"

ai-engine confirm_worker
  → RPOP confirmed_queue
  → check_daily_limit()
  → analyze_signal() (Claude API)
  → enriched.pop("market_ctx")   ← 대용량 데이터 제거
  → LPUSH ai_scored_queue {enriched + human_confirmed=True}
```

### 4-3. VI 눌림목 신호 흐름 (S2)

```
websocket-listener ws_client.py
  → GRP 7 (1h) VI 발동·해제 수신
  → write_vi() → HSET vi:{stk_cd}
  → LPUSH vi_watch_queue {stk_cd, vi_type, ...}

api-orchestrator ViWatchService
  → RPOP vi_watch_queue
  → StrategyService.checkViPullback()
  → SignalService.processSignal()
  → LPUSH telegram_queue
  → (이후 ai-engine 흐름과 동일)
```

### 4-4. 강제청산·일별 리포트 (특수 타입)

```
api-orchestrator
  → LPUSH telegram_queue {type: "FORCE_CLOSE" | "DAILY_REPORT", ...}

ai-engine queue_worker
  → type in ("FORCE_CLOSE", "DAILY_REPORT")
  → LPUSH ai_scored_queue 즉시 통과 (AI 분석 없음)

telegram-bot signals.js
  → FORCE_CLOSE → formatForceClose() → sendMessage
  → DAILY_REPORT → formatDailyReportEnhanced() → sendMessage
```

### 4-5. 뉴스 매매 제어 흐름

```
ai-engine news_scheduler (30분 주기)
  → 뉴스 API 조회 → Claude 분석
  → SET news:trading_control (CONTINUE|CAUTIOUS|PAUSE)
  → 변경 시 LPUSH ai_scored_queue {type: "NEWS_ALERT"}
  → PAUSE 시 LPUSH ai_scored_queue {type: "PAUSE_CONFIRM_REQUEST"}

telegram-bot signals.js
  → NEWS_ALERT → sendMessage (알림)
  → PAUSE_CONFIRM_REQUEST → sendMessage (confirm_pause / cancel_pause 버튼)

사용자 confirm_pause 클릭
  → kiwoom.setTradingControl('PAUSE')
  → POST /api/trading/control/PAUSE
  → Redis news:trading_control = PAUSE

ai-engine queue_worker (신호 처리 시)
  → GET news:trading_control == "PAUSE"
  → result = CANCEL (모든 신호 취소)
```

---

## 5. Human Confirm Gate 흐름 (세부)

이번 세션에서 추가된 핵심 기능. 기존에는 규칙 점수 통과 즉시 Claude API를 호출했으나, 이제는 반드시 사람의 승인이 있어야 Claude 분석이 진행된다.

```
                  ┌───────────────────────────────────┐
                  │     queue_worker (ai-engine)      │
                  │                                   │
  telegram_queue  │  score < threshold                │
  ──────────────► │  ────────────►  ai_scored_queue   │
                  │  (CANCEL)                         │
                  │                                   │
                  │  score >= threshold               │
                  │  ────────────►  human_confirm_queue│
                  └───────────────────────────────────┘
                                       │
                  ┌────────────────────▼──────────────┐
                  │  startConfirmPoller (telegram-bot) │
                  │                                   │
                  │  RPOP human_confirm_queue          │
                  │  sendMessage + InlineKeyboard      │
                  │    ✅ confirm_yes:{sigId}          │
                  │    ❌ confirm_no:{sigId}           │
                  └───────────────────────────────────┘
                                       │
                  ┌────────────────────▼──────────────┐
                  │    사용자 버튼 응답 (callback_query) │
                  │                                   │
                  │  confirm_yes → LPUSH confirmed_q  │
                  │  confirm_no  → DEL pending key    │
                  └───────────────────────────────────┘
                                       │
                  ┌────────────────────▼──────────────┐
                  │   confirm_worker (ai-engine)      │
                  │                                   │
                  │  RPOP confirmed_queue             │
                  │  check_daily_limit()              │
                  │  analyze_signal() → Claude API    │
                  │  pop("market_ctx")                │
                  │  LPUSH ai_scored_queue            │
                  │    human_confirmed = True         │
                  └───────────────────────────────────┘
```

**타임아웃**: 컨펌 대기 시간은 1800초(30분). 이 시간 내 응답 없으면 `confirm_pending:{sig_id}` TTL 만료로 자동 무효화.

---

## 6. 모듈 간 계약 (Interface)

### 6-1. telegram_queue 페이로드 (api-orchestrator → ai-engine)

```json
{
  "id": 12345,
  "stk_cd": "005930",
  "strategy": "S1_GAP_OPEN",
  "action": "ENTER",
  "message": "[S1_GAP_OPEN] 삼성전자 갭 4.2% ...",
  "type": null,
  "gap_pct": 4.2,
  "cntr_strength": 145.0,
  ...전략별 추가 필드
}
```

특수 타입: `type = "FORCE_CLOSE"` | `"DAILY_REPORT"` (AI 분석 없이 통과)

### 6-2. human_confirm_queue 페이로드 (ai-engine → telegram-bot)

```json
{
  ...telegram_queue 원본 필드,
  "rule_score": 75.0,
  "market_ctx": {
    "tick":     {"10": "-20800", ...},
    "hoga":     {"total_buy_bid_req": "12345", ...},
    "strength": 145.2,
    "vi":       {},
    "ws_online": true
  }
}
```

### 6-3. confirmed_queue 페이로드 (telegram-bot → ai-engine)

human_confirm_queue 페이로드와 동일 (confirm_pending 에서 그대로 LPUSH)

### 6-4. ai_scored_queue 페이로드 (ai-engine → telegram-bot)

```json
{
  ...원본 신호 필드,
  "rule_score": 75.0,
  "ai_score": 82.5,
  "action": "ENTER",
  "confidence": "HIGH",
  "ai_reason": "강한 갭 상승에 체결강도 확인...",
  "adjusted_target_pct": 3.5,
  "adjusted_stop_pct": -1.5,
  "human_confirmed": true    // confirm_worker 경로만 존재
}
```

---

## 7. 주요 설정값

### ai-engine 환경변수 및 기본값

| 변수 | 기본값 | 설명 |
|------|--------|------|
| `AI_SCORE_THRESHOLD` | `60.0` | 최소 스코어 (전략별 임계값 우선) |
| `MAX_CLAUDE_CALLS_PER_DAY` | `100` | 일별 Claude API 호출 상한 |
| `POLL_INTERVAL_SEC` | `2.0` | 큐 폴링 간격(초) |
| `AI_HEALTH_PORT` | `8082` | 헬스체크 HTTP 포트 |
| `ENABLE_STRATEGY_SCANNER` | `false` | 독립 전략 스캐너 활성화 |
| `NEWS_ENABLED` | `true` | 뉴스 스케줄러 활성화 |
| `NEWS_INTERVAL_MIN` | `30` | 뉴스 분석 주기(분) |
| `ENABLE_MONITOR` | `true` | 데이터 품질 모니터 활성화 |
| `MONITOR_INTERVAL_SEC` | `60` | 모니터 점검 주기(초) |
| `ENABLE_OVERNIGHT_WORKER` | `true` | 오버나잇 워커 활성화 |

### 전략별 Claude 호출 임계값

| 전략 | 임계값 |
|------|--------|
| S1_GAP_OPEN | 70 |
| S2_VI_PULLBACK | 65 |
| S3_INST_FRGN | 60 |
| S4_BIG_CANDLE | 75 |
| S5_PROG_FRGN | 65 |
| S6_THEME_LAGGARD | 60 |
| S7_AUCTION | 70 |
| S8_GOLDEN_CROSS | 65 |
| S9_PULLBACK_SWING | 60 |
| S10_NEW_HIGH | 65 |
| S11_FRGN_CONT | 60 |
| S12_CLOSING | 65 |
| S13_BOX_BREAKOUT | 65 |

### telegram-bot 환경변수

| 변수 | 기본값 | 설명 |
|------|--------|------|
| `POLL_INTERVAL_MS` | `2000` | ai_scored_queue 폴링 간격(ms) |
| `MIN_AI_SCORE` | `65` | ENTER 신호 최소 AI 스코어 |
| `CONFIRM_POLL_INTERVAL_MS` | `2000` | human_confirm_queue 폴링 간격(ms) |

### Human Confirm Gate 타임아웃

| 항목 | 값 |
|------|-----|
| `confirm_pending:{sig_id}` TTL | 1800초 (30분) |
| ai-engine 측 CONFIRM_TIMEOUT_SEC | 1800초 |
| telegram-bot 측 CONFIRM_PENDING_TTL | 1800초 |

### websocket-listener 환경변수

| 변수 | 기본값 | 설명 |
|------|--------|------|
| `BYPASS_MARKET_HOURS` | `false` | 장 외 시간 연결 허용 (모의 테스트) |
| `KIWOOM_MODE` | `mock` | `real` 또는 `mock` |
| `HEALTH_PORT` | `8081` | 헬스체크 포트 |
| `MAX_RECONNECTS` | `10` | 최대 재연결 횟수 |
| `HEARTBEAT_INTERVAL` | `10` | 하트비트 갱신 주기(초) |

---

## 8. 금번 세션 수정사항

### 8-1. ai-engine/redis_reader.py (수정)

파일 하단에 Human Confirm Gate 관련 함수 3개 추가:

- `push_human_confirm_queue(rdb, item)` : 규칙 점수 통과 신호를 `human_confirm_queue`에 LPUSH하고 `confirm_pending:{sig_id}` 에 SETEX 1800s
- `pop_confirmed_queue(rdb)` : `confirmed_queue` 에서 RPOP 후 JSON 파싱 반환
- `push_confirmed_queue(rdb, payload_str)` : `confirmed_queue` 에 LPUSH (telegram-bot에서 호출 용도, 현재 직접 redis.lpush 방식으로도 사용)

상수 추가: `HUMAN_CONFIRM_QUEUE`, `CONFIRMED_QUEUE`, `CONFIRM_PENDING_PFX`, `CONFIRM_TIMEOUT_SEC`

### 8-2. ai-engine/queue_worker.py (수정)

1. import에 `push_human_confirm_queue` 추가
2. `process_one()` 함수 내 `else` 분기(score >= threshold) 변경:
   - **기존**: Claude API 직접 호출 → ai_scored_queue 발행
   - **변경**: `human_confirm_queue` 에 등록 후 `return True` (confirm_worker가 이후 처리)
3. 코멘트 갱신: "6. 기준 미달(CANCEL) 결과 발행" 명확화

### 8-3. ai-engine/confirm_worker.py (신규 생성)

`confirmed_queue` 를 폴링하여 Claude API 분석 후 `ai_scored_queue` 에 발행하는 워커.

- `process_confirmed(rdb)` : 항목 1개 처리
- `run_confirm_worker(rdb)` : 폴링 루프 (빈 큐 시 최대 5초 대기)
- `market_ctx` 필드 제거 후 발행 (대용량 방지)
- `human_confirmed: True` 필드 추가

### 8-4. ai-engine/engine.py (수정)

- `from confirm_worker import run_confirm_worker` 임포트 추가
- `tasks` 리스트에 `asyncio.create_task(run_confirm_worker(rdb))` 추가 (항상 활성)

### 8-5. telegram-bot/src/handlers/signals.js (수정)

파일 하단에 Human Confirm Gate 폴러 추가:

- `sendConfirmRequest(bot, allowedChatIds, item)` : 신호 내용을 HTML 포맷으로 인라인 키보드(✅/❌)와 함께 발송, `confirm_pending:{sigId}` SET EX 1800
- `startConfirmPoller(bot, allowedChatIds)` : `human_confirm_queue` RPOP 폴링 루프
- `module.exports` 에 `startConfirmPoller` 추가

### 8-6. telegram-bot/src/index.js (수정)

1. import 갱신: `{ startPolling, startConfirmPoller }` 구조분해
2. `const { getClient: getRedis }` 추가 (Human Confirm Gate 콜백에서 Redis 직접 접근)
3. `bot.action(/^confirm_yes:(.+)$/, ...)` 핸들러 추가:
   - `confirm_pending:{sigId}` GET
   - TTL 만료 시 "컨펌 시간 초과" 안내
   - 유효 시 `confirmed_queue` LPUSH → `confirm_pending:{sigId}` DEL → editMessageText
4. `bot.action(/^confirm_no:(.+)$/, ...)` 핸들러 추가:
   - `confirm_pending:{sigId}` DEL → editMessageText "신호 취소"
5. `main()` 함수에서 `startConfirmPoller(bot, chatIds)` 호출 추가

---

## 9. 알려진 제약사항 및 주의점

1. **`confirm_pending` 이중 저장**: `push_human_confirm_queue` 에서 SETEX로 저장하고, `sendConfirmRequest` 에서도 SET EX 한다. 두 번째 SET이 첫 번째를 덮어쓰는데 `market_ctx` 포함 여부가 동일하므로 문제없다. 단, TTL이 sendConfirmRequest 시점부터 재기산됨.

2. **`confirmed_queue` 방향**: ai-engine은 LPUSH → RPOP 순서로 FIFO를 보장한다. telegram-bot의 confirm_yes 핸들러도 `redis.lpush('confirmed_queue', ...)` 방식이므로 일관성 있음.

3. **`market_ctx` 생략 가능성**: `confirmed_queue` 에는 market_ctx 가 포함된 상태로 들어오나, confirm_worker 에서 pop 후 제거하여 ai_scored_queue 에는 포함되지 않는다. Claude 분석 시에는 `ctx = item.get("market_ctx", {})` 로 추출하여 사용한다.

4. **`sig_id` 충돌 위험**: `id` 필드가 없으면 `stk_cd` 를 사용하므로, 동일 종목에서 여러 전략 신호가 동시에 발생하면 `confirm_pending:{stk_cd}` 가 덮어쓰여질 수 있다. id 필드를 항상 포함하도록 api-orchestrator에서 보장하는 것이 권장된다.

5. **`ENABLE_CONFIRM_GATE` 환경변수 없음**: confirm_worker는 항상 기동된다. 컨펌 게이트를 비활성화하려면 현재 코드 수정이 필요하다.

6. **`hibernate.ddl-auto: create`**: application.yml 주의 – 프로덕션 DB에서 실행 시 테이블 재생성 위험.
